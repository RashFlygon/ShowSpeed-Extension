# ShowSpeed

Ninjask-themed speed tools for Pokemon Showdown teambuilder.

Folder layout:
- `manifest.json`
- `scripts/` page logic and content bridge
- `popup/` extension popup UI
- `icons/` extension icons

Load as an unpacked extension in Chrome from this folder.
